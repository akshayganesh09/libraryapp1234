<!DOCTYPE html>
<html lang="en">
<head> 
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">

    
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    
  
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    
    
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>

  
    <link href="css/bootstrap.min.css" rel="stylesheet">
		<link href="//netdna.bootstrapcdn.com/font-awesome/3.2.1/css/font-awesome.min.css" rel="stylesheet">
    <title><%=title%></title>
    <link href="css/style.css" rel="stylesheet">
    <!-- <link rel="stylesheet" type="text/css" href="css/style.css> -->
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <ul class="navbar-nav">
          <li class="nav-item">  
            <a class="nav-link" href="#">HOME</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">FILE</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">VIEW</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
             HELP
            </a>
            <div class="dropdown-menu">
              <a class="dropdown-item" href="#">About</a>
              <a class="dropdown-item" href="#">check for update</a>
              <a class="dropdown-item" href="#">Exit</a>
            </div>
          </li>
        </ul>
        <ul class="nav justify-content-end">
    
          <li class="nav-item">
            <a class="nav-link active" href="http://localhost:5000/signup">SignIn</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="http://localhost:5000/">Library</a>
          </li>
        </ul>
        </nav>
    <div class="login-box">
      <form action="http://localhost:5000/" onsubmit="return validation()">
        <h3>_Library_</h3>
        <h1>LogIn</h1>
        <%for(i=0;i<login.length;i++){%>
        <div class="textbox">
          
            <<%=login[i].type%> type="<%=login[i].input%>" placeholder="<%=login[i].value%>" name="<%=login[i].assign%>" id="<%=login[i].assign%>">
            <!-- <input type="text" placeholder=" User Name" name="user" id="user" autocomplete="off"> -->
            
              <<%=login[i].input1%> id="<%=login[i].tom%>" class="text-danger"></span>
        </div>
        <%}%>
        <br>
        <input type="submit" name="submit" value="submit" class="btn btn-success">
      </form>
    </div>

    <div class="footer" style="left: 0; bottom: 0; margin-bottom: 0px; padding-top: 10px; margin-top: 660px; padding-left: 2px;">

        <h6><strong>Follow Us</strong> <br> <span class="fa fa-2x fa-instagram" aria-hidden="true"></span> <span class="fa fa-2x fa-facebook" aria-hidden="true"></span> <span class="fa fa-2x fa-youtube" aria-hidden="true"></span></h6>
    </div>




    <script type="text/javascript">
 
      function validation(){

          var user = document.getElementById('user').value;
          var pass = document.getElementById('pass').value;
        
          if (user ==""){

              document.getElementById('username').innerHTML = " **please fill the user name";
              return false;
          }
          if((user.length <=2)||(user.length >20)){

              document.getElementById('username').innerHTML = " ** user name must be between 2 to 20 characters";
              return false;
          }
          if(!isNaN(user)){
              document.getElementById('username').innerHTML = " ** user name must not be integers";
              return false;
          }

          // password

          if (pass ==""){

            document.getElementById('pasword').innerHTML = " **please enter the password";
            return false;
            }
          if(pass.length<=6){

            document.getElementById('pasword').innerHTML = " ** password is too short ";
            return false;
          } 
          if(pass.search(/[0-9]/)==-1){

            document.getElementById('pasword').innerHTML = " ** password must contain atleat 1 number";
            return false;
          }
          if(pass.search(/[A-Z]/)==-1){

            document.getElementById('pasword').innerHTML = " ** password must contain atleat 1 uppercase character";
            return false;
          }
          if(pass.search(/[a-z]/)==-1){

            document.getElementById('pasword').innerHTML = " ** password must contain atleat 1 Lowercase character";
            return false;
          }
          if(pass.search(/[!/@/#/$/%/^/&/*/_/+/./?/]/)==-1){

            document.getElementById('pasword').innerHTML = " ** password must contain atleat 1 special character";
            return false;
          } 
      }    
        </script>
        
        <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
        <script src="/public/js/bootstrap.min.js"></script>
</body>
</html>